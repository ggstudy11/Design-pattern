### 概述
**责任链模式**是一种行为设计模式，允许你将请求沿着处理者链进行发送，收到请求后，每个处理者均可对请求进行处理，或将其传递给链上的下一个处理者

### 场景
很常见的一个场景，载用户登录等接口后端开发时，有些需要一系列的身份认证、授权以及参数校验，当后续需要新增校验时，会让这整个校验显得复杂不堪。

### 解决方案
责任链将特定行为转化为被称作处理者的独立对象。在上述实例中，将每个检查步骤都抽离成仅有一个方法的类，责任链模式将这些处理者串成一天链处理，请求在链上移动。
